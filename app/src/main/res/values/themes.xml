<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MyApplication" parent="Theme.Material3.DayNight">
        <!-- Customize your light theme here -->
        <item name="colorPrimary">@color/background_primary</item>
        <item name="colorPrimaryContainer">@color/card_background</item>
        <item name="android:statusBarColor">@color/background_primary</item>

        <!-- Optional: force status bar icons to be dark or light -->
        <item name="android:windowLightStatusBar">true</item> <!-- use false for white text -->
    </style>

    <style name="Theme.MyApplication" parent="Base.Theme.MyApplication" />
</resources>
